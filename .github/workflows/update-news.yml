name: Update News

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "News is generated locally by scripts/generate_news.py and committed with reading updates."
